name: "📚 latest Blog"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */24 * * *" 

jobs:
  update_blogs:
    name: "Update With Latest Blogs"
    runs-on: ubuntu-latest
    steps:
      - name: "📥  Fetching Repository Contents"
        uses: actions/checkout@main

      - name: "📚  Hashnode blogs updater"
        uses: "manikanta528/action-hashnode-blog@1.1.1"
        with:
          USERNAME: "manikanta528" 
          COUNT: 3 
          STYLE: "blog-left"
          BLOG_URL: "https://manikanta528.hashnode.dev/"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
